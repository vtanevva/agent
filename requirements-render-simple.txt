# Core Flask dependencies (minimal for Render)
flask==2.3.3
flask-cors==4.0.0
gunicorn==21.2.0

# Basic utilities
python-dotenv==1.0.0
requests==2.31.0
requests-oauthlib==1.3.1
psutil==5.9.5

# Google API dependencies
google-api-python-client==2.108.0
google-auth==2.23.4
google-auth-oauthlib==1.1.0

# AI dependencies
openai==1.3.0

# Database (optional)
pymongo==4.5.0

# Configuration
pydantic>=2.0.0

# Remove these for Render compatibility:
# pinecone - heavy dependency
# openai - can be added later
# faiss-cpu - heavy ML dependency
# cryptography - can cause build issues
